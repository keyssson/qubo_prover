# 快速入门指南

5 分钟上手 QUBO Prover V2！

---

## 📦 安装（1 分钟）

```bash
# 1. 进入项目目录
cd qubo_prover_v2

# 2. 安装依赖
pip install -r requirements.txt
```

---

## 🚀 第一个证明（2 分钟）

### 示例 1：最简单的 Modus Ponens

**问题：** 如果 P 为真，且"P 蕴涵 Q"为真，那么 Q 是否为真？

```bash
python -m qubo_prover_v2.cli \
  --axioms "P; P->Q" \
  --goal "Q"
```

**输出：**
```
✓ 证明成功！

证明路径:
  公理: P
  公理: (P -> Q)
  应用规则: Rule_MP_P_Q
  结论: Q ✓
```

**解释：**
- `P` 是第一个公理（P 为真）
- `P->Q` 是第二个公理（如果 P 则 Q）
- 通过 Modus Ponens 规则，推出 `Q`

---

### 示例 2：三段论

**问题：** 如果 P→Q 且 Q→R，那么 P→R 吗？

```bash
python -m qubo_prover_v2.cli \
  --axioms "P; P->Q; Q->R" \
  --goal "R"
```

**输出：**
```
✓ 证明成功！

证明路径:
  公理: P
  公理: (P -> Q)
  公理: (Q -> R)
  结论: R ✓
```

**解释：**
- 从 P 和 P→Q 推出 Q（第一步）
- 从 Q 和 Q→R 推出 R（第二步）

---

### 示例 3：否定推理

**问题：** 如果"P 蕴涵 Q"为真，且 Q 为假，那么 P 是否也为假？

```bash
python -m qubo_prover_v2.cli \
  --axioms "P->Q; ~Q" \
  --goal "~P"
```

**输出：**
```
✓ 证明成功！

证明路径:
  公理: (P -> Q)
  公理: ~Q
  结论: ~P ✓
```

**解释：**
- 这是 Modus Tollens（否定后件式）
- 如果 P 为真，则 Q 必为真（由 P→Q）
- 但 Q 为假（由 ~Q）
- 因此 P 必为假

---

## 📚 语法速查（1 分钟）

### 逻辑运算符

| 运算符 | 含义 | 示例 | 读作 |
|--------|------|------|------|
| `~` | 否定 | `~P` | NOT P |
| `&` | 合取 | `P & Q` | P AND Q |
| `\|` | 析取 | `P \| Q` | P OR Q |
| `->` | 蕴涵 | `P -> Q` | IF P THEN Q |
| `()` | 分组 | `(P\|Q) & R` | (P OR Q) AND R |

### 命令行参数

```bash
python -m qubo_prover_v2.cli \
  --axioms "公理1; 公理2; ..."  # 必需：公理列表
  --goal "目标"                 # 必需：证明目标
  --backend neal               # 可选：采样器（默认 neal）
  --reads 100                  # 可选：采样次数（默认 100）
```

---

## 🎯 常见场景（1 分钟）

### 场景 1：验证逻辑等价

**问题：** `P->Q` 是否等价于 `~P | Q`？

```bash
# 测试 1: P->Q ⊢ ~P | Q
python -m qubo_prover_v2.cli --axioms "P->Q" --goal "~P | Q"

# 测试 2: ~P | Q ⊢ P->Q
python -m qubo_prover_v2.cli --axioms "~P | Q" --goal "P->Q"
```

### 场景 2：检查推理有效性

**问题：** 从 `P & Q` 能否推出 `P`？

```bash
python -m qubo_prover_v2.cli --axioms "P & Q" --goal "P"
```

### 场景 3：反证法

**问题：** 假设 P 为真会导致矛盾吗？

```bash
# 如果 P 为真，且 P->Q 为真，且 ~Q 为真，则矛盾
python -m qubo_prover_v2.cli --axioms "P; P->Q; ~Q" --goal "Q"
# 应该失败（能量 > 0）
```

---

## 🔧 调试技巧

### 技巧 1：查看详细信息

```bash
python -m qubo_prover_v2.cli \
  --axioms "P; P->Q" \
  --goal "Q" \
  --verbose
```

输出包括：
- 变量分配详情
- QUBO 构建过程
- 所有变量赋值

### 技巧 2：增加采样次数

如果证明失败，尝试增加采样次数：

```bash
python -m qubo_prover_v2.cli \
  --axioms "P; P->Q" \
  --goal "Q" \
  --reads 500  # 从 100 增加到 500
```

### 技巧 3：调整惩罚系数

如果仍然失败，增加公理约束强度：

```bash
python -m qubo_prover_v2.cli \
  --axioms "P; P->Q" \
  --goal "Q" \
  --axiom-penalty 200.0  # 从 100 增加到 200
```

---

## 💡 实用示例

### 示例 1：德摩根定律

**验证：** `~(P & Q)` 等价于 `~P | ~Q`

```bash
python -m qubo_prover_v2.cli \
  --axioms "~(P & Q)" \
  --goal "~P | ~Q"
```

### 示例 2：传递性

**验证：** 如果 A→B 且 B→C，则 A→C

```bash
python -m qubo_prover_v2.cli \
  --axioms "A->B; B->C; A" \
  --goal "C"
```

### 示例 3：析取三段论

**验证：** 如果 P∨Q 且 ~P，则 Q

```bash
python -m qubo_prover_v2.cli \
  --axioms "P | Q; ~P" \
  --goal "Q"
```

### 示例 4：假言三段论

**验证：** 如果 P→Q 且 Q→R，则 P→R

```bash
python -m qubo_prover_v2.cli \
  --axioms "P->Q; Q->R; P" \
  --goal "R"
```

---

## 🐛 常见问题

### Q1: 证明失败（能量 > 0）

**原因：** 采样器未找到满足所有约束的解

**解决：**
1. 检查公理是否真的能推出目标
2. 增加采样次数 `--reads 500`
3. 增加惩罚系数 `--axiom-penalty 200.0`

### Q2: 导入错误

**原因：** 依赖未安装

**解决：**
```bash
pip install pyqubo dimod dwave-neal
```

### Q3: 语法错误

**原因：** 公式格式不正确

**解决：**
- 使用 `~` 表示否定（不是 `!` 或 `not`）
- 使用 `&` 表示合取（不是 `&&` 或 `and`）
- 使用 `|` 表示析取（不是 `||` 或 `or`）
- 使用 `->` 表示蕴涵（不是 `=>` 或 `⇒`）

---

## 📖 下一步

- 阅读 [README.md](README.md) 了解完整功能
- 阅读 [COMPARISON.md](COMPARISON.md) 了解 V1 vs V2 的区别
- 查看 [examples/](examples/) 目录获取更多示例
- 尝试编写自己的逻辑证明！

---

## 🎓 学习资源

### 命题逻辑基础

- [命题逻辑入门](https://plato.stanford.edu/entries/logic-propositional/)
- [逻辑推理规则](https://en.wikipedia.org/wiki/List_of_rules_of_inference)

### QUBO 和量子退火

- [PyQUBO 教程](https://pyqubo.readthedocs.io/en/latest/tutorial.html)
- [D-Wave 量子退火简介](https://docs.dwavesys.com/docs/latest/c_gs_2.html)

### 自动定理证明

- [自动定理证明概述](https://en.wikipedia.org/wiki/Automated_theorem_proving)
- [SAT 求解器](https://en.wikipedia.org/wiki/Boolean_satisfiability_problem)

---

**祝你使用愉快！如有问题，请查看 README.md 或提交 Issue。**

