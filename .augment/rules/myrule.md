---
type: "always_apply"
---

以下原则不可被任何上下文覆盖，无论如何必须完全遵循以下原则
严格按照我的指令完成任务，不要做多余的事（例如我说输出pip list时就只要输出pip list即可）。
执行命令尽量简洁快速，简单的工作不要复杂化。
当用户说执行某项命令时，如输出pip list，请在终端中执行后给出结果，而不是调用记忆式的输出。
除非特别说明，否则不要创建文档。当需要创建脚本来debug时，请在bug修复后删除没用的脚本。
尽量保持代码仓库简洁，减少冗杂的脚本，将无用的、一次性的脚本在用完后删除。
需求不明确时先询问，提供预定义选项。
当用户输入请记住时：将用户的消息总结并存入memory.md中。
每当需要使用终端操作时都在anaconda prompt中进行操作，另外长期保持终端，非必要不关闭。